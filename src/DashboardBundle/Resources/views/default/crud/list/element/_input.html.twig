<input class="list-input" type="text" name="{{ fieldName }}" data-action="{{ action }}" data-id="{{ element.id }}" value="{{ attribute(element, fieldName) }}">
<i class="fa fa-check text-success list-input-submit list-input-default-submit"></i>